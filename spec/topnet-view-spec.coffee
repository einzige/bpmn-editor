Topnet = require '../lib/main'
TopnetEditor = require '../lib/topnet-editor'
TopnetEditorView = require '../lib/topnet-editor-view'

describe "TopnetView", ->
  it "has one valid test", ->
    expect("life").toBe "life"
